name: Recovery2 Build

on:
  workflow_dispatch:
    inputs:
      # OF源码分支替换原来manifest，URL固定OF官方
      MANIFEST_URL:
        description: 'OrangeFox Manifest(固定不用改)'
        required: true
        default: 'https://gitlab.com/OrangeFox/Manifest.git'
      MANIFEST_BRANCH:
        description: 'OF源码分支'
        required: true
        default: 'fox_12.1'
      DEVICE_TREE_URL:
        description: 'DEVICE_TREE_URL'
        required: true
        default: 'https://github.com/xyyyico/device-5g-L39_IVVI_4_64_V80M60BP_NZW_BT30.git'
      DEVICE_TREE_BRANCH:
        description: 'DEVICE_TREE_BRANCH'
        required: true
        default: 'main'
      DEVICE_PATH:
        description: 'DEVICE_PATH'
        required: true
        default: 'device/5g/L39_IVVI_4_64_V80M60BP_NZW_BT30'
      COMMON_TREE_URL:
        description: 'COMMON_TREE_URL (空则不填)'
        required: false
        default: ''
      COMMON_TREE_BRANCH:
        description: 'COMMON_TREE_BRANCH'
        required: false
        default: ''
      COMMON_PATH:
        description: 'COMMON_PATH (空则不填)'
        required: false
        default: ''
      DEVICE_NAME:
        description: 'DEVICE_NAME'
        required: true
        default: 'L39_IVVI_4_64_V80M60BP_NZW_BT30'
      MAKEFILE_NAME:
        description: 'MAKEFILE_NAME(OF为twrp_设备名)'
        required: true
        default: 'twrp_L39_IVVI_4_64_V80M60BP_NZW_BT30'
      BUILD_TARGET:
        description: 'BUILD_TARGET (recovery / boot)'
        required: true
        default: 'boot'

jobs:
  build:
    if: github.event.repository.owner.id == github.event.sender.id
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Display Parameters
        run: |
          echo "::group::Parameters"
          echo "Manifest: ${{ inputs.MANIFEST_BRANCH }}"
          echo "Device: ${{ inputs.DEVICE_NAME }}"
          echo "Makefile: ${{ inputs.MAKEFILE_NAME }}"
          echo "Target: ${{ inputs.BUILD_TARGET }}"
          echo "::endgroup::"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Cleanup
        uses: rokibhasansagar/slimhub_actions@main

      - name: Prepare Build Env(OF专用环境)
        run: |
          sudo apt update && sudo apt upgrade -y
          git clone https://gitlab.com/OrangeFox/misc/scripts
          cd scripts
          sudo bash setup/android_build_env.sh

      - name: Setup Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Setup SSH
        if: startsWith(inputs.MANIFEST_URL, 'git@') || startsWith(inputs.DEVICE_TREE_URL, 'git@') || startsWith(inputs.COMMON_TREE_URL, 'git@')
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Install Repo
        run: |
          mkdir -p ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod +x ~/bin/repo
          sudo ln -sf ~/bin/repo /usr/bin/repo

      - name: Init OrangeFox Repo
        run: |
          mkdir -p workspace
          cd workspace
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          repo init --depth=1 -u ${{ inputs.MANIFEST_URL }} -b ${{ inputs.MANIFEST_BRANCH }}

      - name: Repo Sync OF Source
        run: repo sync -c -j$(nproc) --force-sync --no-clone-bundle
        working-directory: workspace

      - name: Clone Device Tree
        run: git clone ${{ inputs.DEVICE_TREE_URL }} -b ${{ inputs.DEVICE_TREE_BRANCH }} ./${{ inputs.DEVICE_PATH }}
        working-directory: workspace

      - name: Clone Common Tree
        if: inputs.COMMON_TREE_URL != '' && inputs.COMMON_PATH != ''
        run: git clone ${{ inputs.COMMON_TREE_URL }} -b ${{ inputs.COMMON_TREE_BRANCH }} ./${{ inputs.COMMON_PATH }}
        working-directory: workspace

      - name: Setup Swap
        uses: pierotofy/set-swap-space@master
        with:
          swap-size-gb: 14

      - name: Enable CCache
        run: |
          echo "USE_CCACHE=1" >> $GITHUB_ENV
          echo "CCACHE_DIR=$HOME/.ccache" >> $GITHUB_ENV
          ccache -M 10G
        working-directory: workspace

      # OF编译命令
      - name: Build OrangeFox
        run: |
          source build/envsetup.sh
          export ALLOW_MISSING_DEPENDENCIES=true
          export SKIP_ABI_CHECKS=true
          lunch ${{ inputs.MAKEFILE_NAME }}-eng
          make clean
          mka ${{ inputs.BUILD_TARGET }}image -j$(nproc)
        working-directory: workspace

      - name: Rename OF img
        run: |
          cd workspace/out/target/product/${{ inputs.DEVICE_NAME }}
          mv OrangeFox-*.img ${{ inputs.BUILD_TARGET }}.img
        working-directory: ${{ github.workspace }}

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            workspace/out/target/product/${{ inputs.DEVICE_NAME }}/${{ inputs.BUILD_TARGET }}.img
            workspace/out/target/product/${{ inputs.DEVICE_NAME }}/*.zip
          name: OrangeFox-${{ inputs.DEVICE_NAME }}-${{ github.run_id }}
          tag_name: ${{ github.run_id }}
          body: |
            ✅ OrangeFox编译成功
            OF分支: ${{ inputs.MANIFEST_BRANCH }}
            设备: ${{ inputs.DEVICE_NAME }}
            输出: ${{ inputs.BUILD_TARGET }}.img
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
